// Экспорт функционального компонента AboutPage по умолчанию
export default function AboutPage() {
   // Возвращаем JSX разметку компонента
   return (
      // Корневой div элемент
      <div>
         {/* Заголовок первого уровня */}
         <h1>О нас</h1>
         {/* Параграф с описанием приложения */}
         <p>Учебное приложение на React + TypeScript. CRUD постов, роутинг, базовая стилизация.</p>
      </div>
   );
}

/* ================= ПОЯСНЕНИЯ К КОММЕНТАРИЯМ =================

1. СТРУКТУРА КОМПОНЕНТА:
   - Функциональный компонент AboutPage представляет страницу "О нас"
   - Компонент не принимает пропсов (параметров)
   - Возвращает JSX разметку

2. JSX РАЗМЕТКА:
   - <div>: Блочный контейнер для группировки элементов
   - <h1>: Заголовок первого уровня (самый важный)
   - <p>: Параграф текста с описанием приложения

3. СОДЕРЖИМОЕ СТРАНИЦЫ:
   - Заголовок "О нас" указывает на назначение страницы
   - Текст описывает технологии и функциональность приложения:
     * React + TypeScript - основные технологии
     * CRUD постов - операции создания, чтения, обновления, удаления записей
     * Роутинг - навигация между страницами
     * Базовая стилизация - оформление интерфейса

4. ПРОСТОТА КОМПОНЕНТА:
   - Компонент является презентационным (stateless)
   - Не содержит логики состояния (useState)
   - Не использует побочных эффектов (useEffect)
   - Не выполняет запросов к API

5. СЕМАНТИЧЕСКАЯ РАЗМЕТКА:
   - Используются семантические HTML теги для улучшения доступности
   - Заголовок h1 указывает на главную тему страницы
   - Параграф p содержит поясняющий текст

6. ЛОКАЛИЗАЦИЯ:
   - Весь текст на русском языке
   - Соответствует требованиям русскоязычного веба
*/


// =============================================================================
// КОНЕЦ ФАЙЛА
// =============================================================================
